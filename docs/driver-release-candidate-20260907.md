# Driver 배포 통합 후보 — 2026-09-07

## 범위

- 연결 이슈: `EVNSolution/clever-change-control#240`, `EVNSolution/clever-driver-app#1`.
- 후보 브랜치: `cc-240-driver-unlisted-feature-candidate`.
- 기준: `origin/dev`의 `d43c72f` (#55 병합).
- 완성 소스: `ec17905` tree + 기존 iOS 제출 커밋 `99e02bb`의 기능 변경.
- 이 작업은 후보 통합·검증만 수행한다. 버전 변경, 서명 바이너리 생성, 스토어
  제출 및 서버 배포는 수행하지 않는다. 최종 배포는 `배포: Android, iOS` 채팅 소관이다.

## 포함

- 배송 탭에서 상차 완료·배송 시작, 배송 완료와 사진 증빙 처리.
- 탭 변경 중에도 완료·증빙 상태 유지, 중복 요청 차단과 마지막 배차 완료 재시도.
- 큰 시스템 글꼴에서 배송지·주소 우선 표시 및 지도·순서 편집 공간 대응.
- #55의 같은 배송지 분할 주문 묶음 이동, 지도 접기와 스크롤은 기준 브랜치에 포함.
- #56의 선택 배송일·배차 ID·경로 토큰 전달, 배차 변경 시 주문 목록 상태 초기화.
- `99e02bb`의 배차 선택 전·배차 없음 화면 당겨서 새로고침과 정식 앱 아이콘.
  아이콘 SHA-256: `f876b391aebf7f8feda6e70cdcb6dd661eebb818175dd8a5b28724a879f19510`.
- 인증 PR #54 (`c9cab496`)는 기준 계보에 이미 포함되어 중복 적용하지 않는다.

## 제외 및 서버 의존성

- 15분 정차 후 출발 자동 완료, 설정 ON/OFF와 자동 순서 저장은 앱 미구현이다.
- 문의사항 화면과 API 연동은 앱 미구현이다. 이 두 기능을 출시 기능으로 안내하지 않는다.
- #56 앱 컨텍스트 수정만으로 과거 날짜 배송 교환 누락이 해결되지는 않는다.
  서버 #390 선행 배포 및 실제 과거 날짜 목록·반납·확보·전달 검증이 필요하다.
- 배포 담당의 이번 상태 회신: 서버 #390 `68719c3` CI green, 미병합·미배포.
  문의 초안은 `codex/dsv-driver-inquiries-draft-20260907`의 `db45e7b`로 별도
  보존됐고 배포 금지다. 자동 완료·순서 저장 보강도 미구현이다.
- 인증 서버 PR #387 `d979a6a`는 운영 `ef20cc5` 계보에 포함됐다는 배포 담당
  회신을 받았다. 이번 후보 작업에서는 운영 로그인 E2E를 수행하지 않았다.

## 원본 및 dirty 보존

- 기존 일반 워크트리는 `cc-240-delivery-usability-no-deploy`의 `bc26612`로 보존.
  유일한 미추적 파일 `docs/pending-driver-features.md`를 이 커밋에 기록해 clean으로
  정리했다. 요청·서버 전달 이력 문서는 후보에서 제외했다.
- 일반 기능의 코드 원본 `ec17905`와 #55/#56 원본 브랜치를 재작성·삭제하지 않았다.
- `99e02bb`의 세 기능 파일만 수동 결합했으며 제출 증적 문서 `4e21f8d`/`816f4e0`,
  기존 iOS 워크트리와 스토어 설정은 배포 담당이 별도로 보존·정리한다.
- 버전·패키지·lockfile·EAS 설정은 기준 브랜치와 동일하다. 새 바이너리용 버전과
  빌드 번호 증가는 배포 담당이 최종 후보에서 한 번 수행한다.

## 검증 경계

- 후보에서 전체 테스트 132/132, typecheck, lint, Expo 정합성과 diff 검사 통과.
- Android export 1,304 modules, iOS export 1,289 modules 통과. 빌드 실행기는
  exit 0, errors 0, 잔류 소유 프로세스 0을 기록했다.
- 로컬 export 증거:
  `~/.codex/build-hygiene/logs/driver-unlisted-feature-candidate/20260907T110337.820682+0900-d43c72fabe03-unlisted-feature-candidate/summary.json`.
- 네이티브 기기의 큰 글꼴·드래그·당겨서 새로고침·사진 선택 모달 동작은 미검증이다.
- export 성공은 서명 앱 설치나 스토어 제출 성공을 의미하지 않는다.
